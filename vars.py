import datetime
import re

#data de azi
now= datetime.datetime.now()
dayTemp= str(now.day)
monthTemp= str(now.month)
yearTemp= str(now.year)
hourTemp= str(now.hour)
minuteTemp= str(now.minute)

fwd_start = 0
fwd_end = 0
fwd_time = 0
fin = True

